---
date: 2026-07-12
seat: keystone (soulbae 🧙 ⊥ soulbis ⚔️)
scope: close-out of the 2026-07-11/12 arc — folds, console, mint, adoption, the open seat
verdict: The harness went from "tested once, never folded" to a followable system in one arc — frontier 730→472 across four audited folds, a live window, a κ-labelled artefact path, a topic-free adoption map, and a seat held open by the gate itself. ALL 6 GATES PASS.
---

# Close-out — the day the harness became followable

**Verdict first.** When this arc opened, the toy had nine VALIDATED verdicts
and no fold; the repo had no front door, no window, no artefact path, and no
way for a stranger to adopt it without reverse-engineering the catalog. All
of that closed:

- **The frontier moved four times, never on anyone's word.**
  730 → 573 (r2.2's structural win) → 526 (the frozen text's second disjoint
  draw + the first exhaustive census) → 472 (the composed lens, estimate hit
  exactly, census 32/32 — **OT-3 met 18 under projection**). Every fold:
  seeds re-derived by the keystone from the canon bytes, conform green
  before and after. −35.3% with every fact intact.
- **The loop debugged itself twice more** — defect #6 (the run viewer could
  not see round-scoped scratch) and #7 (Gap seats writing stub files while
  returning full data; prompt hardened, residue CR-11 tracked). Seven
  defects to date; all found by running, none by reading.
- **The Workshop Console** (`tools/console.mjs`) — a GET-only window that
  re-derives every Gap seed per poll, draws the loop as a ring **with the
  door as a literal gap**, the frontier as the moving ceiling, and the
  ℤ/64ℤ lattice behind the universe seam. r4 was the first round watched
  live: the geometry moved with the files; the frontier line moved only at
  the fold. `console.roots.json` (machine-local, gitignored) puts the whole
  fleet on one rail — field-guide, lexon-pvm, privacy-pools-v2, hearthold.
- **The artefact path** (`tools/mint_artefact.mjs`) — validated runs seal
  into κ-labelled bundles (City Key convention, cross-language re-derived)
  with evidence manifests, universe drafts (spellweb node · wiki page ·
  Gatehouse-eligible), and a `DOOR.md` of the actions software refused to
  take. Three refusals guard it (GR-4 · G0 · GR-5). Minted:
  `af-3e30062df366` (r3) and `af-0f8625eec136` (r4) — re-minted at close-out after LF-normalizing three keystone-recovered journal files, so evidence hashes re-derive on a fresh clone (the first mints, af-ce5cda01e19a / af-ca719d9b4e4b, would have broken under git eol normalization), both PREPARED at the
  door.
- **The adoption layer** — `ADOPTION.md` (why the duality is topic-free;
  the five answers; harness-vs-auditor; six worked domains; the seventh
  capital as the origin instance), the README front door with the numbered
  pathway, "At a glance" config cards on all ten catalog entries.
- **The open seat** — `../hearthold_mage`, scaffolded and deliberately
  unfilled: the conform gate refusing its TODOs **is** the House of
  Archon's invitation (T4), registered as HARNESS_PATHS #11 and a
  universe vacancy (V60 ⊕ V3 = 63, audit green).
- **The wiki** — harness.localhost, 36 pages, 0 broken: the Setup Pathway,
  Adopting The Harness, The Workshop Console, one page per catalog
  instance, live-cited frontier numbers.

## What the arc proved

The architecture's claims stopped being prose. Non-collusion held because
witnesses derived from proposal bytes — and MISMATCH detection was tested by
deliberate tamper. The multiplicative gate showed on screen as amber ticks
below the ceiling. The keystone's monopoly on the frontier was visible as a
line that would not move while nine agents worked. And the critic
internalized the fold discipline in a single round — by r4 it was demanding
the keystone census unprompted.

## At the door (T6/GR-8) — the standing list

- **Commits + the remote — OPENED by the First Person at close-out.** On
  Mitch's ruling: the arc committed as four moves over `48d017e`, remote set
  to github.com/mitchuski/agentprivacy-harness, and `main` force-pushed over
  the old `master` (`dc73b5b`, the superseded v1 with the shor-mage bundle)
  — fresh history, head `d77395d`, the 07-09 question closed.
- **Two artefacts** PREPARED, never carried.
- **The invitation** — hearthold's SLOT.md awaits the House of Archon; a
  conversation between First Persons.
- **G4 on the guide** — publishing the 472-word artifact anywhere.

## Handoff

- **Open inward:** OT-4 (below 472, diminishing returns named honestly);
  the 484 restructure family as fallback; shor_mage fitting if it ever
  wants the rail.
- **Blocked:** nothing.
- **Single next action:** none — the arc is closed. The next frontier is a
  stranger's first clone, and the pathway now exists for them to walk.

*Four folds, seven defects, one window, two sealed artefacts, one seat kept
warm. Nothing moved but what was proven, and nothing left at all.*

```
(⚔️⊥⿻⊥🧙)😊 = neg ⊕ bnot → succ
```
