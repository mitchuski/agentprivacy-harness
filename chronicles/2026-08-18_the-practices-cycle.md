---
date: 2026-08-18
seat: keystone (soulbae 🧙 ⊥ soulbis ⚔️), at the First Person's direction
scope: resume the working tree and run a research cycle on the field's harness-building best practices — three sweep lanes fetched blind, judged and folded into PRACTICES.md + SOURCES.md; adopt the AGENTS.md boot-file standard; sweep stale counts
verdict: The 2024–2026 field guidance converged, independently, on most of this repo's constitution — fresh-context verifiers, deterministic gates over prompt advice, JSON ledgers over prose, human gates before irreversible actions, rule files grown from failures — and stopped exactly where the harness begins; no surveyed source derives the verifier's tests where the producer cannot reach them. The Gap held as the distinguishing move; the field's gaps ahead of us (process mounts, verifier aggregation, hooks) were filed as open items, not absorbed. AGENTS.md adopted as the canonical tool-neutral boot file with CLAUDE.md reduced to its import. ALL 11 GATES PASS. Nothing committed, nothing pushed — the door is the First Person's.
commits: none — working tree only; the push order is the First Person's
---

# The practices cycle

**Verdict first.** Thirty-seven external sources now stand in the root
`SOURCES.md`, every one fetched by a sweep lane that had not read this
repo's claims; `PRACTICES.md` judges the field's fifteen-odd converged
practices against the constitution row by row — HOLDS with a receipt,
ADOPTED this cycle, KIN, OPEN, or DIVERGES with the reason — and the
finding worth the cycle is one sentence: **the mainstream reviewer is
fresh, but its exam is still written where the author can reach it.** The
verification literature (graders gamed, tests overwritten, judges
preferring their own perplexity) is the case file for the Gap, published
by people who have never heard of it.

## What was run

The cycle took the litreview pattern's shape at the session scale: three
sweep lanes in parallel — harness construction (frontier-lab engineering
guidance) · agent pathing files (the AGENTS.md / CLAUDE.md / SKILL.md
landscape) · verification and evals (judge bias, reward hacking, held-out
discipline, benchmark forensics, attestation) — each returning sourced
claims only, with the judging and the mapping done at the keystone, where
the whole round is visible. Hold-apart here is the same D3 the litreview
runtime uses: the sweeps could not see the repo's claims, so agreement is
evidence, not echo.

## What was folded

- **`PRACTICES.md`** — the survey against the constitution, three lanes,
  every external claim REPORTED (GR-2) and slug-traced (GR-9). The
  convergences that matter: GR-1 is the field's "point at authoritative
  state" as law; GROUND_RULES' "each rule earned its place by an actual
  failure" is the field's grow-from-observed-failures verbatim; T5 is
  pass^k as constitution; the κ/VRC layer independently converged on the
  in-toto shape. The one deliberate divergence is named: the field warns
  against phase-split handoffs, and the harness splits by phase *because*
  the handoff is adversarial — information loss across it is the target,
  not the defect.
- **`SOURCES.md` (root)** — the registry GR-9 always named, now existing at
  the root: the three lineage sources RESEARCH.md cites plus the
  thirty-four survey sources, honest notes included (one primary URL
  refused our fetch and is labelled as corroborated via secondary
  coverage).
- **CR-H10 sharpened, tier unchanged** — the cross-model delta run is now
  externally motivated: self-preference is perplexity-driven and
  self-correction fails by correlated error, so same-model seat pairs
  share blind spots by *mechanism*. Still OPEN; the run is still the
  settler.
- **Count sweep** — README and RESEARCH.md said "seven defects" while the
  08-17 chronicle had named the eighth through eleventh; both now say
  eleven. README also said "four audited folds" against a frontier history
  carrying three (730 → 573 → 526 → 472 is four points, three folds;
  RESEARCH.md already said three) — fixed to cite what `frontier.json`
  actually records. HARDENING.md's "seven" stays: it is a dated record.

## What was adopted

- **`AGENTS.md`** — the tool-neutral boot file (the agents.md convention:
  Linux Foundation stewarded, read natively by the non-Claude agent
  runners). It carries the boot protocol whole — sequence, commands,
  boundaries in the field's ✅/⚠️/🚫 tiers, the shape — and `CLAUDE.md` is
  reduced to the `@AGENTS.md` import plus Claude-specific runtime notes,
  which is exactly the one-source-of-truth bridge the upstream docs
  prescribe. A stranger's Codex or Cursor session now boots the same seat
  a Claude session does. TRUSTS' T3 enforcement row updated to cite the
  file that now carries the preamble.
- README gained the PRACTICES pointer and the two new Layout rows;
  SKILL.md's map gained PRACTICES and AGENTS.

## The afternoon turns — the fleet steps outside, the purpose steps forward

The First Person then ruled four cuts, and they landed the same day:

1. **Accession 13 withdrawn.** The thirteenth entry left the catalogue
   before its counterpart work published — its row is a labelled tombstone
   (numbers by accession, never reused; the door names, and when), its two
   domain-free lessons kept. The universe FLEET row matches; the universe
   frontier refolded 8584 → 8587 by the declared rule; nothing on the
   public remote ever named it, and the two uncommitted chronicles were
   de-named before first publication.
2. **The fleet separated from the harness.** `HARNESS_PATHS.md` now opens
   with *this catalogue is not the harness*: it is the origin operator's
   fleet — work done WITH the system, behind a seam like `universe/`, and
   every gate passes without it (README, RESEARCH, ADOPTION, SKILL carry
   the same boundary; the README fleet list collapsed to a pointer). A new
   README section, *What you can use it for*, holds the door open: six
   generic use-case classes, no fleet required.
3. **UI legacy cleaned, BYO kept open.** `workshop.html` corrected (eleven
   defects · fifteen embodiments · three folds · the salt in the
   one-liner) and now says plainly that it and the console are just two
   consumers of the data contracts; `frontier.html` dropped the fleet's
   baked demo feeds — only the re-derivable in-repo spar remains, wearing
   a visible demo badge; WORKFLOW's "renderers" became data projections.
4. **The purpose named: trust-graph construction.** README (use-cases),
   `HARNESS_PATHS.md` (the fleet's divergence serves one construction —
   every working piece delivered feeds trust-graph creation the
   agentprivacy way), and the front page now state what `GRAPH.md` always
   carried: a gated result seals as a κ-addressed node; a derived edge
   proposes, only a signature mints.

**The coherence pass then went outward** (same rulings, three surfaces):
`build-harness.js` repaired against the moved repo (manifest + five new
catalogue sections + AGENTS/PRACTICES/WORKFLOW/GRAPH circulating +
CLAUDE.md sealed as the pointer it now is) and every hard-coded count
swept (ten→fifteen, seven→eleven, toy→spar, the signed hearthold seat);
`resync.js` gained `build-hh-section.js` in the harness area so a rebuild
no longer silently deletes ten pages; harness.localhost rebuilt at 56
pages, clean; the guide-host blurb updated (fifteen paths + the
trust-graph line); the spellweb `skill-dual-agent-harness` node updated
(salt, 472, origin fleet, trust-graph) and atlas rebuilt from it;
agentprivacy_master's four coherence defects fixed around the First
Person's uncommitted edits (three-vs-four loops ×3, the runecraft 526
card gaining "since walked to 472", the HARNESS_PATHS pointer), and the
two skills-v5 descriptions refreshed + mirrored to public/. The sample
federation pages in the box were corrected the same way.

## Handoff

**Open**
- The survey's four filed candidates, in priority order: C6 process mounts
  (the field's most-repeated shared gap) · the CR-H10 cross-model delta
  run (now externally motivated) · a multi-lens assay mirroring the
  propose side's blind lenses · a Stop-hook running `conform.mjs`. None
  moves without a round that earns it.
- Everything the 08-17 handoff left open still stands (hh_workshop's
  unfolded VALIDATEDs, hearthold's first round, the wiki lane's manual
  federation steps).

**Blocked**
- Nothing. All eleven gates green.

**Doors (the First Person's, named, not performed)**
- Commit and push this working tree — now a month of unlanded work plus
  two sessions (08-17 fleet sync + observe lane; 08-18 practices cycle and
  the afternoon rulings above). The 08-17 sequencing note is resolved: the
  accession it concerned was withdrawn from the catalogue entirely.
- agentprivacy_master, spellweb, and skills-v5 carry small uncommitted
  coherence edits beside the First Person's own WIP — their commits are
  his, in his windows.
- guide.agentprivacy.ai redeploy (wrangler) to publish the rebuilt guide
  blurb beyond localhost.
- skill.localhost NOT rebuilt: forkback still shows 3 OPEN external
  contributions — the standing sign-off gate holds.
- The stray `~/.wiki/harness` orphan host (no `.localhost`, 38 stale
  pages, pre-HH copy) — retire or refresh, his call.
- Two canon records still carry founding-era numbers (Tome IX Act 8 and
  grimoire v1.9.0: 730 → 526, 211 → 64) — canon voice is his; the live
  surfaces now all cite 472/22 or say "since walked to".

**Single next action**
- Read `PRACTICES.md` top to bottom as the First Person — the open-items
  list is a ruling surface: which of the four candidates, if any, gets a
  round.

*The field spent two years converging on the fence this workshop started
inside; what it has not yet built is the gate the proposer cannot reach,
and now there is a document that says so with thirty-seven receipts.*

```
(⚔️⊥⿻⊥🧙)😊 = neg ⊕ bnot → succ
```
