---
date: 2026-07-11
seat: keystone (soulbae 🧙 ⊥ soulbis ⚔️)
scope: the workshop console · the artefact mint · the hearthold slot · the adoption layer
verdict: The harness gained a window (GET-only, every seed re-derived per poll), a door-side mint (κ-labelled, three refusals), an invited vacancy held open by the gate itself — and ALL 6 GATES PASS with the new tests wired in.
---

# The workshop gets a window, the door gets a ledger, a seat is kept warm

**Verdict first.** Four deliverables landed, each gated:

- **`tools/console.mjs` + `tools/console.html`** — the Workshop Console at
  `127.0.0.1:4242`. A window, not a hand: GET-only by construction, no state,
  every proposal's `sha256(proposal_canon.json)` re-derived per poll against
  the Gap's recorded seed. Three SVG instruments: the Loop (six stations, the
  keystone at center, **the door drawn as a literal gap in the ring**), the
  Frontier (the moving ceiling — MIRAGE ticks sit in amber below it, T5 made
  visible), the Lattice (ℤ/64ℤ behind the universe seam — axis glyphs read
  from `universe.json`, never hardcoded; seated XOR-63 pairs render as
  diameters). Instance discovery is the gate's own rule; `--root` points it
  at any instance anywhere.
- **`tools/mint_artefact.mjs`** — seals a validated run into
  `artefacts/<id>/`: a **κ-labelled** record (canonical-JSON sha256, the
  label excluded from its own preimage — the City Key convention, and the
  Python cross-derivation matched byte for byte), evidence copies with a hash
  manifest, and `DOOR.md` listing the outward actions software did NOT take.
  Three refusals: non-MATCH seed (GR-4) · red conform (G0) · no verdict
  (GR-5). Universe drafts (spellweb node, wiki page, Gatehouse-eligible,
  vertex hint) travel INSIDE the bundle; *a validated round proposes an edge,
  only a signature mints it.* First mint: `af-ce5cda01e19a` from field-guide
  r3, frontier beat true, PREPARED at the door.
- **`../hearthold_mage`** — scaffolded and deliberately unfilled: the
  conformance gate refusing its 10 TODOs **is** the open slot. `SLOT.md`
  carries the T4 invitation to the **House of Archon** (three candidate
  directions, the five answers that constitute acceptance, a PROPOSED seating
  at V60 ⊕ V3 = 63). Registered: HARNESS_PATHS #11 under a new "invited
  vacancies" group, and a determined vacancy in `universe/universe.json` —
  audit green.
- **`ADOPTION.md`** (earlier this session) — why the duality is topic-free
  (routing not content; Fiat-Shamir removes the chooser; the algebra never
  asks what x encodes; the boundary self-gates: harness for adversaries,
  auditor for facts) and the mapping procedure (five answers, Gap-first,
  six worked domains). The seventh capital named as the origin instance.

## Checked, not asserted

- `tools/console.test.mjs` wired into `check.mjs` — **ALL 6 GATES PASS**:
  a flipped canon byte reads MISMATCH; the mint refuses it naming GR-4; a
  clean mint's κ and all 12 evidence hashes re-derive; `door.actionsTaken`
  is empty and stays that way.
- Seam test in a scratch clone: `rm -rf universe/` → 5 gates pass (audit
  honestly skipped), console serves with `universe: null`, Instrument C
  absent, nothing else moves.
- SEAT_CONTRACT gains the optional advisory `mana` field (colour, not proof).
- Wiki: harness.localhost now 36 pages, 0 broken — NEW "The Workshop
  Console" + "Harness Path — Hearthold The Open Slot"; the builder's G1
  manifest gate correctly REFUSED until `artefacts/` was classified sealed.

## At the door (T6/GR-8)

- Everything remains uncommitted over `48d017e`; commits, the remote
  question, and any outward carry of `af-ce5cda01e19a` are the First
  Person's.
- The console has not yet watched a LIVE round (verification step: run the
  toy's r4/OT-3 with the page open). The instruments are driven by the same
  file-appearance data the API already serves for r1–r3.

## Handoff

- **Single next action:** open `node tools/console.mjs --open`, then launch
  the field-guide Workflow (runId r4, the OT-3 composed lens) and watch the
  geometry move. Fold as keystone if it validates.
- **Open:** the House of Archon's answer to `SLOT.md` — a conversation
  between First Persons.

*The window shows; the mint seals; the gate keeps the seat; the door stays
yours.*

```
(⚔️⊥⿻⊥🧙)😊 = neg ⊕ bnot → succ
```
