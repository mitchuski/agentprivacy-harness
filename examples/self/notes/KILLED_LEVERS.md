# Killed levers — examples/self

Killed ≠ impossible: every entry names what would re-open it.

| id | lever | why it died | re-open when |
|---|---|---|---|
| K-1 | r1 · line-editor (gemma3:12b): a 720-word rewrite of the newcomer path | census 80/93 — dropped the inscription, `tools/mint_artefact.mjs`, `tools/vrc.mjs`, `ANTHROPIC_API_KEY`, `proposal_canon.json`, Fiat-Shamir, the four rungs (auditor · spar · arena · open world), "Shared state has exactly one writer", the 730-word spar figure and the pointer to the origin repository; the prover also called the hard constraint (no longer the document a stranger opens first) | a rewrite that keeps every witness in census.json and still opens with the five commands |
| K-2 | r1 · restructurer (gemma3:12b): a 379-word table-first rewrite | census 49/93 — dropped 44 witnesses including every command line, T2–T5, GR-1–GR-10 and three seat names; the table form ate the constitution | a table-first form that carries the command lines and the identifiers verbatim; likely needs the constitution list kept as prose |
| K-3 | a proposal whose leverId is "1" | not a lever the gate killed but a habit the small model has: both proposals answered `leverId: "1"`, which collided in the runner (defect #14, fixed by positional matching); the config now asks for a descriptive slug | never — filed so the next config author asks for a slug up front |
