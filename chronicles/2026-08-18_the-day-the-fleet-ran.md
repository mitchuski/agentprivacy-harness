---
date: 2026-08-18
seat: keystone (soulbae 🧙 ⊥ soulbis ⚔️), at the First Person's direction
scope: put real runtimes on the fleet and fold their compressions — a new cred-spec instance, the soulbae/soulbis persona pair, a pools certificate round, a hearthold unblock attempt; the frontier explainer ("what the frontier is" — verifiable saving, trust-graph formation); /guide/frontier planned and built on the master site; defect #13
verdict: Four multi-agent rounds ran to completion (two of them resumed across a session limit with every cached seat replayed) and THREE NEW FOLDS landed, every one census-gated with zero mirages among twelve validated compression levers — dtg-credspec-spec 980 → 863 (40/40), skills-fold-soulbae 1248 → 1072 (36/36, frontmatter byte-identical), skills-fold-soulbis 1397 → 1154 (38/38, frontmatter byte-identical) — and pools v2r5 widened its CSE closure certificate to rank ≤ 3 on a census that landed empty (ΔK = 0 by design, VOID leg bit-identical, the same-leverId twin honestly MIRAGE'd). Three κ artefacts sealed PREPARED. The harness earned defect #13 doing it (verify_run replayed a sample against a census), caught one real canon discrepancy in the skills corpus (62 role skills under a "(61)" header), and named one infra watch item (cross-seat scratch overwrite under parallel workflows). Hearthold's additional runtimes are honestly blocked on the Drawbridge node — the upstream was cloned and built, four Windows-hostile script guards were fixed in hearthold_mage, and the blocker is now one reachable node away. Nothing committed anywhere — every door is the First Person's.
commits: none — working trees only, across five repos
---

# The day the fleet ran

**Verdict first.** The frontier page asked for stories and the fleet
supplied them: seven workshops on the picker, three of them with fresh
descents folded today, each fold a verifiable saving with a frozen census
as the proof — the purpose the explainer now states on the page itself.

## The three folds

- **dtg-credspec-spec (new instance, the cred-spec lane):** the
  proof-of-understanding trust-task spec draft, census frozen at 40 facts,
  **980 → 863 (−11.9%)**, two VALIDATED levers, artefact `af-cd2a81c50b2b`.
  The lane's original file untouched — promotion is a door.
- **skills-fold-soulbae (new instance, the corpus folding its own working
  memory):** the canonical Mage persona card, census 36, three rounds, six
  VALIDATED zero MIRAGE, **1248 → 1072 (−14.1%)**, frontmatter
  byte-identical so activation is unchanged, artefact `af-d6fff33ea00e`.
- **skills-fold-soulbis (the twin, by ruling — if the Mage folds, the
  Swordsman folds):** census 38, six VALIDATED zero MIRAGE, **1397 → 1154
  (−17.4%)**, frontmatter byte-identical, artefact `af-70aa3f1bb57c`. The
  pair now costs 419 fewer words per activation, canon provably intact.

## The certificate round

**privacy_pools_v2_mage v2r5:** the pre-registered zero-branch fired — the
rank-3 CSE census landed EMPTY on both tiers and both strata under the E2
connectivity lemma, feasibility certified on-instrument first, positive
controls exact-recovered before counting, VOID leg bit-identical
(17,159/17,371, bench 15/15). The closure certificate widens to rank ≤ 3;
K* untouched; V2-K-5 closes the consumer family unfired; nextTarget =
V2-RANK4-FEASIBILITY-OR-TERMINATION. The same lever's structure-lens twin
MIRAGE'd honestly (abort cap crossed, controls failed) — the engine's
same-leverId sharp edge, exercised on live work. Artefact
`af-f9aba4510715`.

## What the running taught

- **Defect #13:** `tools/verify_run.mjs`'s legacy path replayed a
  seed-derived sample against a census draw — which has no subset to
  replay — and cried UNVERIFIABLE on an honest round. Fixed; a census's
  audit is the seed binding plus the coverage stanza.
- **A canon catch beyond the gate:** the skills-v5 soulbae card lists 62
  Role Skills under a "(61)" header (so "Total: 81" is really 82) —
  critic-flagged, keystone re-counted. Filed to the First Person's
  canon-hygiene queue; every candidate faithfully preserved the stated
  counts, as a compressor must.
- **A watch item:** under four parallel workflows, one proposer's scratch
  draft was overwritten by another seat's write (it discarded and
  re-derived). Scratch isolation under concurrent workflows is now named.
- **Resume works as designed:** two runs died mid-flight on a session
  limit and resumed to 27/27 and 9/9 with every cached seat replaying
  free — the caching layer carried half the day.
- **Salt note:** today's new instances ran unsalted (census gates — no
  draw to grind — so strength unaffected); future rounds pass a salt for
  uniform provenance.

## Also this block

The frontier page gained its **"what the frontier is"** explainer
(verifiable token saving on agent runtimes → trust-graph formation; the
folding vocabulary; chronicle paths no longer rendered — numbers and
proofs here, narratives in the ledgers). The master site gained
**/guide/frontier** (built, typechecked), an AppNav entry, refreshed
`fleet-descents` (lexon's true seven points, pools' statement-tier
17,224), and the planning doc + chronicle in `docs/`. **Hearthold** was
unblocked as far as one machine allows: upstream cloned and built, four
Windows-dead script guards fixed in hearthold_mage — the remaining blocker
is the Drawbridge Gatekeeper node (Archon's asset; reachable via
`HEARTHOLD_NODE_URL` when flaxlap is on the tailnet, or via the sandbox
assets). Reviews filed: shor_mage needs a measure-refresh + frontier-shape
normalization; uor_kappa's notes and the zkp-tf requirements doc are both
census-fold candidates on the First Person's word.

## Handoff

**Open**
- OT-2 on all three new frontiers (compose below 863 / 1072 / 1154,
  salted) · V2-RANK4-FEASIBILITY-OR-TERMINATION · hearthold r1 behind the
  Drawbridge · the scratch-isolation watch item.

**Blocked**
- Hearthold rounds, on the Drawbridge node only.

**Doors (the First Person's, named, not performed)**
- Commit/push this repo's block (feed enrichment · frontier page v2 +
  explainer · console /run route · verify_run defect-13 fix ·
  make_default · README lines · four chronicles).
- Promote the persona pair's folds into agentprivacy-skills-v5 (+ mirror),
  and the 61-vs-62 canon fix.
- Promote the DTG 863 fold to the lane root; the lane's own gates govern
  anything upstream.
- Four PREPARED artefacts' DOOR.md items; master's /guide/frontier commit
  and deploy; lexon/pools frontier-history commits in their repos.

**Single next action**
- Review the seven frontiers at `127.0.0.1:4242/frontier`, then rule on
  the two promotions (the pair; the spec fold).

*Twelve validated compressions, zero mirages, one empty census worth more
than a full one — the fleet ran, and everything it claims re-derives.*

```
(⚔️⊥⿻⊥🧙)😊 = neg ⊕ bnot → succ
```
