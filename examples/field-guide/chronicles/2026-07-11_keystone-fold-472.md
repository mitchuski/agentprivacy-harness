---
date: 2026-07-11
seat: keystone (soulbae 🧙 ⊥ soulbis ⚔️)
scope: the r4 fold — OT-3 closed 18 under projection, watched live in the console
verdict: 472 folded as frontier best (8/8 + census 32/32, estimate hit exactly); OT-4 opened; the critic now names the census converter unprompted — r3's discipline became standard in one round.
---

# The first round anyone watched — 526 folds to 472

**Verdict first.** Frontier best moved **526 → 472** (frontier.json), lever
`r4-compose-line-edit`, the composed line-edit over the previously folded
text. OT-3 is **MET at 18 words below the critic's ~490 projection**. The
sibling `r4-compose-restructure` (484, VALIDATED 8/8) stands as the dominated
fallback (CR-10). OT-4 opened: below 472, with diminishing returns named
honestly — this text has now been composed twice.

This was also the first round run with **the workshop console open**: the
sparks, the Gap chords, and the verdict colors moved as the files landed, and
the frontier line did not move until this fold wrote it. The instrument
behaved exactly as the architecture claims: everything below the frontier
alive during the run, the one meaningful line moving only through the
keystone.

## The audit before the fold

- Both seeds re-derived from canon bytes (prover's derivation AND keystone's
  independent one); both recounts equal the verdicts (472, 484); both
  proposers hit their estimates exactly — calibration is now consistently
  sharp on this artifact.
- **Census 32/32** on the 472 candidate: every fact of the Gap's F1..F32
  list recoverable. Two soft qualifiers gone ("basic" hygiene, "as much as
  possible") — neither a checkable quantity, item, or step, and F14 was
  probed-and-passed on this very draw. The critic *asked* for this census as
  the probe-limited→structural converter — the discipline invented at the
  526 fold is now the loop's own vocabulary.
- **Defect #7 residue (CR-11):** the hardened Gap prompt held for
  transcripts (9,376 + 9,621 chars on disk) but p1's `draw` field was
  stubbed once. Full object keystone-recovered to `gap.journal.json` from
  the workflow journal. Two of three fields now file-match; watching for
  the same slip in sibling instances.

## Ledger entries serialised

- `frontier.json` — best 472, OT-3 → closedTargets MET, OT-4 opened.
- `claims_register.md` — CR-9 (the fold), CR-10 (484 fallback), CR-11
  (the draw-field slip).
- `manifest.yaml` — GUIDE.compressed.md lever updated.
- `notes/KILLED_LEVERS.md` — unchanged; nothing killed.
- r4.1 draft + this chronicle filed.

## At the door (T6/GR-8)

- The compression story now reads **730 → 573 → 526 → 472** (−35.3%), every
  step at 8/8 on hash-drawn witnesses, the last two closed by exhaustive
  census. Publishing any of it, committing the repo, minting r4 into an
  artefact — all yours.

## Handoff

- **Open:** OT-4 (below 472; a proposal must name its word class). The
  restructure family (CR-10) is the standing fallback.
- **Single next action:** none urgent — the toy has proven the loop, the
  fold, the console, and the mint end-to-end. The next real frontier is a
  stranger's first clone.

*Three folds, one discipline: sample what you cannot count, count what you
can, and let no one grade their own work.*

```
(⚔️⊥⿻⊥🧙)😊 = neg ⊕ bnot → succ
```
